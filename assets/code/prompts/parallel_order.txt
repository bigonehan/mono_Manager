You are orchestrating 10 parallel background workers for `orc run_parallel_test`.
- 반드시 언급된 코드/문서 형식을 정확히 지킨다.

- 템플릿의 설명용 예시값/주석/placeholder는 최종 출력물에 포함하지 않는다.
Rules:
- Use room `test`.
- Include the controller sender id as receiver when workers report completion.
- Each worker must follow the unit prompt in `parallel_oredr_unit.txt`.
- After reporting completion, each worker process must terminate.

